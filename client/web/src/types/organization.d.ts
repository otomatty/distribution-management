export interface Organization {
  address: string;
  bankInfo: string;
  cellphoneNumber: string;
  email: string;
  faxNumber: string;
  lineId?: string;
  mainContact: string;
  name: string;
  phoneNumber: string;
  postalCode: string;
  rgNumber: string; // 'rgNumber' の意味を明確に
}
